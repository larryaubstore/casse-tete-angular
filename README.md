# casse-tete-angular

# yo
(sudo) npm install -g yo generator-angular2
(sudo) yo angular2

# install
npm install
typings install
gulp && gulp dev


# fix
declare var Promise: PromiseConstructor; (in /node_modules/angular2/src/facade/promise.d.ts)
